{
  "name": "seabrick",
  "license": "UNLICENSED",
  "version": "0.3.2",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ seabrick",
    "create-local": "graph create --node http://localhost:8020/ seabrick",
    "remove-local": "graph remove --node http://localhost:8020/ seabrick",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 seabrick",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.78.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
